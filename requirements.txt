# Core data engineering
pandas>=2.0
numpy>=1.24
SQLAlchemy>=2.0
psycopg2-binary>=2.9      # PostgreSQL driver
python-dotenv>=1.0        # load secrets from .env
PyYAML>=6.0               # read config.yaml

# Analysis / notebooks (Milestone 1)
jupyter>=1.0
matplotlib>=3.7
seaborn>=0.12

# Machine learning (Milestones 5-6)
scikit-learn>=1.3
mlflow>=2.9               # experiment tracking + model registry

# Serving (Milestone 8)
streamlit>=1.30

# Monitoring (Milestone 9)
evidently>=0.4

# Testing
pytest>=7.4

# Orchestration (Milestone 7) is added later:
#   on Windows, Apache Airflow runs best via Docker or WSL.
#   We'll decide Airflow-in-Docker vs Prefect when we reach M7.
